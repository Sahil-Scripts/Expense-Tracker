# Backend - Expense Tracker (Express + MongoDB)
## Setup
1. Copy `.env.example` to `.env` and fill `MONGO_URI` and `JWT_SECRET`.
2. `cd backend && npm install`
3. `npm run dev` (requires nodemon) or `npm start`
Server runs on PORT (default 5000).
